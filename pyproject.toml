[tool.poetry]
name = "scaleupdown"
version = "0.1.0"
description = ""
authors = ["MalvaTalpa "]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
requests = "*"
pandas = "^2.2.3"
fabric = "^3.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
